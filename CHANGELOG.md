# 更新日志

本文件记录 `astrbot_plugin_arknight_gacha_simulator` 的每次版本更新内容，供插件市场更新预览使用。

版本号遵循语义化版本（SemVer），按 `vMAJOR.MINOR.PATCH` 递增。

---

## [v0.4.4] - 2026-09-19

> **本版本汇总了自 `v0.3.1` 以来的全部变化。**
> 此前发布的 `v0.4.0` ~ `v0.4.3` 因存在严重缺陷已全部撤回（yanked），
> 请停留在 `v0.3.1` 及更早版本的用户直接升级到本版本。

### 新增

- **单抽结果图全面重制**（元素表驱动）：背景 / 干员立绘 / 阵营 Logo / 带文字职业图标 / 星级五角星 / 中英文姓名 / 装饰星与光柱 / 暗角打光 / 底部渐变。所有元素的位置、大小、透明度、混合模式与层级集中在 `Script/composer_config.py` 中调整。
- **双语文字渲染**（`Script/text_render.py`）：中文与英文由同一字体的 CJK / 拉丁字形自动分工，支持描边 + 填充、字距、可变字重。
- **阵营 Logo 映射**（`Script/camp_logo_map.py`）：覆盖 46 个阵营，阵营为空或映射未命中时统一降级为罗德岛。
- **装饰星随机化**：每个副本独立随机尺寸与位置，并按概率 180° 翻转；翻转后的装饰星可单独设置色调与高度偏移，其附属光柱自动跟随同色。
- **程序化柔光柱**：支持平顶比例、横纵向衰减、透明度与滤色混合，尺寸随装饰星等比缩放。
- **单抽立绘画质档位**：可在插件配置页选择 `original` / `high` / `medium` / `low`，各档位独立缓存，默认原画质。
- **带文字职业图标**预拉取、**卡池封面本地化缓存**（30 天 TTL，不再把 URL 交给下游加载）。

### 修复

- **从插件市场安装 / 更新后插件无法加载**
  同一进程内残留的同名模块会让 `import composer_config` 命中旧对象，从而缺少新增接口并中断加载。现在加载前先清理本插件用到的同名模块缓存，并把插件目录强制置于 `sys.path` 最前。
- **全新安装后卡池数据无法自动拉取**
  运行时数据子目录不存在，导致抓取脚本写入失败（`[Errno 2]`）、卡池数据始终拉不下来、抽卡引擎无法初始化。现在插件入口与各数据脚本都会先确保目录存在。
- **图片中人物、头像与图标全部缺失**
  `set_data_dir()` 注入插件专属数据目录时遗漏了 4 个缓存子目录常量，使「写入目录」与「读取目录」永久错位 —— 在 AstrBot 中运行时头像、职业图标、立绘全部读不到（本地调试脚本不经过该路径，因此不易察觉）。
- **单抽图没有干员立绘**
  立绘按画质档位写入子目录，读取时却只在根目录查找，导致始终 miss 并静默跳过绘制。
- **卡池数据文件损坏会导致插件永久不可用**
  读取异常会穿透初始化流程，使插件一直停在「数据未加载」且不自愈。
- **首次安装后指令可能长时间无响应**
  抽卡引擎初始化会在事件循环中同步拉起子进程（最长 60 秒），期间整个 AstrBot 卡住。现在该操作移到线程，且指令路径不再触发。
- **图片合成阻塞事件循环**
  单抽 / 十连的合成是同步 CPU 操作，会冻结所有用户与其它插件，现已移到线程执行。
- **已结束的卡池仍可继续抽**
  内存中的卡池列表不会随时间刷新；旧池结束后仍留在列表里且仍可被抽中。
- **跨年欢庆池「首次 6★ 必定未持有」规则未生效**
  `owned_characters` 参数始终传空，导致该保护分支永远不成立。

### 优化

- 数据尚未就绪时（首次安装）会在**后台主动触发一次拉取**，并向用户返回明确提示，而不是让用户干等或反复重试。
- 自动更新器的启动任务改为**持有引用**，避免其在网络等待期间被垃圾回收，导致首次数据拉取静默失败。
- 立绘解码结果加入 **LRU 缓存**，连续抽卡时无需重复解码同一张立绘。
- 引擎初始化日志分级：首次运行数据尚未就绪时不再输出 `error` 级堆栈（属预期流程）。

### 运行时数据

- 运行时数据（下载缓存、抓取数据、字体、SQLite 数据库）统一存放于框架分配的插件专属目录 `data/plugin_data/<插件名>/`，既不污染 AstrBot 的 `data/` 根目录，也不写入会被更新覆盖的插件安装目录。
- 检测到历史遗留数据时会**自动迁移**，老用户升级后不会丢失抽卡次数、签到记录与潜能仓库。
- 字体**不随插件包分发**，首次运行时从 OpenHarmony 官方仓库下载并做 SHA-256 校验（详见 [`THIRD_PARTY_NOTICES.md`](THIRD_PARTY_NOTICES.md)）。

---

## [v0.3.0] - 2026-08-09

### 新增
- **卡池计数器功能**：单抽 / 十连的结果中新增"本池累计抽数"统计，显示该用户在当前卡池的累计抽卡次数。

---

## [v0.2.4] - 2026-08-09

### Bug 修复
- **修复首次运行卡池查询为空的时序问题**：初始化时自动更新器异步生成数据，命令可能先于数据生成触发。
- `_check_loaded` 在 `active_pools` / 抽卡引擎为空时先尝试从磁盘刷新，避免"当前没有进行中的卡池"。

---

## [v0.2.3] - 2026-08-09

### 新增
- 新增 `CHANGELOG.md`，记录各版本更新内容，供插件市场更新预览。

---

## [v0.2.2] - 2026-08-09

### 重构 / 变更
- **移除内置 `data/` 目录**：不再随仓库提交任何卡池数据，`data/` 全部由插件首次运行时从 GitHub / PRTS 自动拉取并初始化。
- `.gitignore` 忽略整个 `data/` 目录。
- **`auto_update` 默认开启**（`false → true`），并让 `_start_updater` 真正尊重该配置。
- 首次运行会自动拉取 `gacha_table.json` 并生成卡池数据，实现开箱即用。

---

## [v0.2.1] - 2026-08-09

### Bug 修复
- **修复启动时 `raise` 错误**：首次运行 / 数据缺失时，`_load_pool_data` 不再直接抛 `FileNotFoundError` 导致初始化中断。
- 改为容忍数据缺失（`self.pools = []`），由自动更新器首次拉取生成数据后经回调重新加载。

---

## [v0.2.0] - 2026-08-09

### 重构 / 新增
- 图片合成模块迁移至 `Script/image_composer.py`，并补齐缺失素材（如 `star_light.png`），修复四星图紫色光效丢失问题。
- 修复限定池识别正则，`车辙与风的归所` 等限定池正确进入 `LIMITED` 分支（6★ UP 70%，限定+陪跑各 35%）。
- 数据库改用 AstrBot 官方 `get_astrbot_plugin_data_path()`，解决不同环境（桌面版 / Docker / 自定义 root）下初始化失败的问题，数据库存于标准插件数据目录。
- 重写 `README.md`，更新 `metadata.yaml`，完善 `.gitignore`。
- 完整素材（背景、星级光效、卡底、3D 模型）随仓库提交。
