{
  "data_path": {
    "type": "string",
    "description": "cleaned_pools_final.json 的路径。留空则使用插件内置的 data/processed/ 目录",
    "default": "",
    "hint": "一般无需填写，插件会自动使用内置数据"
  },
  "auto_update": {
    "type": "bool",
    "description": "启动时是否自动从 GitHub / PRTS 更新卡池数据(首次运行会拉取数据初始化)",
    "default": true,
    "hint": "默认开启。首次运行会自动拉取 gacha_table.json 并生成卡池数据"
  },
  "sign_in_amount": {
    "type": "int",
    "description": "每日签到赠送的抽卡次数",
    "default": 10,
    "hint": "每日 /抽卡签到 可领取的抽卡机会次数"
  }
}
